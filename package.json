{
  "name": "track-attack-running",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "node scripts/build-with-git-info.js",
    "prebuild": "node scripts/build-with-git-info.js",
    "serve": "npm run build && python3 -m http.server 8080",
    "dev": "npm run build && python3 -m http.server 8080"
  },
  "dependencies": {
    "tailwindcss": "^3.4.7"
  }
}
